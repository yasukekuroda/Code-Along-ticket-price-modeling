describe("BestPlanCalculator", () => {
  describe("calculate", () => {
    test("シネマシティズン会員の高校生が土日の20時までに映画を見る場合、高校生料金になる", () => {
      expect(1 + 1).toBe(2); // fixme. dummy.
    });
  });

  describe("障害者手帳を持つ80歳の人が平日に映画を見る場合、障害者(学生以上)料金になる", () => {
    expect(2 + 3).toBe(5); // fixme. dummy.
  });
});
